name: example_project
description: A sample project where the coverage excludes generated files
version: 1.0.0
homepage: https://www.example.com
author: Jack Warren <jack@jackwarren.info>

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  built_value: ^8.0.4

dev_dependencies:
  remove_from_coverage:
    path: ../
  pedantic: ^1.11.0
  test: ^1.16.8
  build_runner: ^1.12.2
  built_value_generator: ^8.0.4
  test_cov: ^1.0.1
